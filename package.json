{
  "name": "gh-remove-pr-username",
  "version": "1.0.0",
  "description": "A browser extension that removes the `username:` prefix in branch names shown in GitHub pull request headers.",
  "scripts": {
    "check": "node --check content.js",
    "verify:live-pr": "node scripts/verify-live-pr.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "puppeteer-core": "^24.26.1"
  }
}
